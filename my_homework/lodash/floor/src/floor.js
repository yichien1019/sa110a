export function floor(number, [precision=0]){
    var num
    return Math.floor(num*Math.pow(10,precision)) / Math.pow(10,precision)
}

//     var y
//     var ans
//     ans=Math.floor(y*Math.pow(10,precision)) / Math.pow(10,precision)
//     return ans
// }

// function getnum(num){
//     var num;

// }
//     return Math.floor(Math.floor(number * Math.pow(10, (precision || 0) + 1)) / 10) / Math.pow(10, (precision || 0));
// }
//     var a,b;
//     var number,final;
//     var pow0,pow1;
//     pow0=Math.pow(10,(precision||0))
//     pow1=Math.pow(10,(precision||0)+1)
//     final=Math.floor(Math.floor(number*pow1)/10)/pow0
//     return final
// }
//     var number;
//     var 


// Math.floor(number*Math.pow(10,(precision || 0))) / Math.pow(10,(precision || 0))

// }